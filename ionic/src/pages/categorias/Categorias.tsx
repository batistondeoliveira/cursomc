import Container from "components/container/Container";

const Categorias: React.FC = () => (
  <Container header={{title: 'Categorias', to: '/'}}>
    <p>Página de categoria</p>    
  </Container>
)

export default Categorias;
